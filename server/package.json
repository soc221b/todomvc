{
  "private": true,
  "name": "server",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "serve": "json-server ./db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.0"
  }
}
